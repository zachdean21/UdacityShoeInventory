<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:fontFamily">@font/jockey_one</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="InputTextStyle" parent="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_marginLeft">@dimen/large_margin</item>
        <item name="android:layout_marginRight">@dimen/large_margin</item>
    </style>

    <style name="LoginButtonStyle">
        <item name="android:fontFamily">@font/jockey_one</item>
    </style>

    <style name="ShoeTextStyle">
        <item name="android:fontFamily">@font/jockey_one</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="TitleTextStyle" parent="ShoeTextStyle">
        <item name="android:textSize">@dimen/title_text_size</item>
    </style>

    <style name="SubtitleTextStyle" parent="ShoeTextStyle">
        <item name="android:textSize">@dimen/subtitle_text_size</item>
    </style>

    <style name="ShoeDetailLabel" parent="ShoeDetail">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ShoeDetailValue" parent="ShoeDetail">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="ShoeDetail">
        <item name="android:fontFamily">@font/jockey_one</item>
        <item name="android:textSize">@dimen/detail_text_size</item>
    </style>

</resources>
